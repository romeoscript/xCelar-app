# Copy to .env and set per environment.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8001

# Optional. When set, rider maps use the real Google map (Directions Embed API).
# Enable "Maps Embed API" in Google Cloud and paste the key here.
EXPO_PUBLIC_GOOGLE_MAPS_KEY=
