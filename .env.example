# Copy to .env and set per environment.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8001

# Maps are native (react-native-maps): Apple Maps on iOS, Google Maps on
# Android. No key needed in Expo Go. For store builds, put a Google Maps
# (Android SDK) key in app.json under android.config.googleMaps.apiKey.
